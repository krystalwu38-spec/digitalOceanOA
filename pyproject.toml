[project]
name = "digitalocean-oa"
version = "0.1.0"
description = "Secure file upload API with signed temporary download links"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
